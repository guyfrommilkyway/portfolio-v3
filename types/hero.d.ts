interface HeroProps {
  headline: string;
  title: string;
  description: string;
  location: string;
}

interface SocialProps {
  name?: string;
  username?: string;
  link: string;
  icon: Component;
}
